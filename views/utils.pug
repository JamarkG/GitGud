mixin ValidationErrorSummary(errors)
  if errors
    div(class='alert alert-danger edit-post-errors' role='start')
      p The following errors occurred:
      ul
        each error in errors
          li= error
